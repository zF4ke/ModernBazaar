package com.modernbazaar.core.strategy.flipping;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class FlippingScorerTest {

//    @Test
//    void score_usesFlowAndChurnAverages() {
//        FlippingScorer scorer = new FlippingScorer(0.005);
//        FlippingScorer.Inputs in = new FlippingScorer.Inputs(
//                100.0, 105.0,   // ib, is
//                1000.0,         // flowAvg (instaBought+instaSold)
//                50.0, 60.0      // churn buy/sell
//        );
//        FlippingScorer.Score s = scorer.score(in);
//
//        assertEquals(5.0, s.spread(), 1e-9);
//        assertEquals(0.05, s.spreadPct(), 1e-9);
//        assertEquals(1000.0, s.flowAvg(), 1e-9);
//        assertEquals(110.0, s.churn(), 1e-9);
//        assertTrue(s.competitionPenalty() > 1.0);
//        assertTrue(s.balanced() > 0);
//        assertTrue(s.delta() > 0);
//    }
}

